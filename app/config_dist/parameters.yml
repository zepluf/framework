parameters:
  kernel.config_dir: "%kernel.root_dir%/config"
  kernel.cache_dir: "%kernel.root_dir%/cache"

  plugins.root_dir: "%kernel.root_dir%/plugins"

  store.http_server: "http://demo.local"
  store.https_server: "https://demo.local"
  store.enable_ssl: false
  # relative path from the site root to the store folder
  store.relative_dir: ""

  store.zencart_dir: "%kernel.root_dir%/../vendor/zencart"
  store.zencart_backend_dir: "%kernel.root_dir%/../vendor/zencart/manager"
  # path to the frontend languages folder
  store.frontend.languages_dir: "%kernel.root_dir%/frontend/languages"
  # path to the frontend templates folder
  store.frontend.templates_dir: "%kernel.root_dir%/frontend/templates"
  store.frontend.current_template: "0007"
  # path to the backend languages folder
  store.backend.languages_dir: "%kernel.root_dir%/backend/languages"
  # path to the backend templates folder
  store.backend.templates_dir: "%kernel.root_dir%/backend/templates"
  store.backend.current_template: "template_default"

  # relative path from the site root to the store folder in case the site uses ssl
  # this should be the same with the relative_dir unless the site is using shared ssl
  store.ssl_relative_dir: ""
  # absolute path to the website root dir (usually the public_html)
  store.root_dir: "%kernel.root_dir%/../web"

  # absolute path to the "web" dir
  web.dir: "%kernel.root_dir%/../web"
  # the relative path from the site root to the "web" folder
  web.relative_dir: ""
  web.cache_dir: "%web.dir%/cache"
  web.images_dir: "%web.dir%/images"

  web_profiler.debug_toolbar.class: Zepluf\Bundle\StoreBundle\EventListener\WebDebugToolbarListener

  locale: en
  database_driver:   pdo_mysql
  database_host:     localhost
  database_port:     ~
  database_name:     demo
  database_user:     simple_shop
  database_password: simple_shop
  table_prefix:      zen_

  mailer_transport:  smtp
  mailer_host:       localhost
  mailer_user:       ~
  mailer_password:   ~

  locale:            en
  secret:            asd214322dewd1e121eq
